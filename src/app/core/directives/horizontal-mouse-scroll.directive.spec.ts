import { HorizontalMouseScrollDirective } from './horizontal-mouse-scroll.directive';

describe('HorizontalMouseScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new HorizontalMouseScrollDirective();
    expect(directive).toBeTruthy();
  });
});
